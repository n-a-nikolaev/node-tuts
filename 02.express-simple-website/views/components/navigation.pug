nav.navbar.navbar-expand-lg.navbar-dark.bg-dark.fixed-top
    .container
        a(class='navbar-brand', href='javascript: void(0);') Express
        button(
            class='navbar-toggler', 
            type='button', 
            data-toggle='collapse', 
            data-target='#navbar-responsive', 
            aria-controls='navbar-responsive', 
            aria-expanded='false',
            aria-label='Toggle navigation'
        )
            span.navbar-toggler-icon
        #navbar-responsive.collapse.navbar-collapse
            ul.navbar-nav.ml-auto
                li.nav-item(class={active: route === 'home'})
                    a(class='nav-link', href='/') Home
                li.nav-item(class={active: route === 'services'})
                    a(class='nav-link', href='/services') Services
                li.nav-item(class={active: route === 'contact'})
                    a(class='nav-link', href='/contact') Contact